export const environment = {
  production: false,
  // We point to the local proxy path, not the real IP directly
  apiUrl: '/sap/bc/srt/scs/sap/zrfc_login_vendor_863', 
  sapClient: '100',
  
  // Credentials for Basic Auth
  apiUser: 'K901863',
  apiPassword: 'Nithish@2004'
};